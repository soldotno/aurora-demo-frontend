/**
 * Dependencies
 */
const React = require('react');

/**
 * Component
 */
const Loading = React.createClass({
  render() {
    return (
      <div>Loading...</div>
    );
  }
});

module.exports = Loading;
